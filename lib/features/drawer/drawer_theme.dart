import 'package:flutter/material.dart';

class DrawerThemeConfig {
  static const accentColor = Color(0xFFFF5A5F);
  static const selectedIndicatorColor = accentColor;
  static const iconSelectedColor = accentColor;
  static const iconHoverColor = Colors.black;
}
